apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-socket-pvc
  namespace: lunaris-databases
spec:
  storageClassName: microk8s-hostpath
  volumeName: redis-socket-pv-vol
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
