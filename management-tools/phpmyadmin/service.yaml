apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin-service
  namespace: lunaris-management-tools
spec:
  selector:
    app: phpmyadmin
  type: ClusterIP
  sessionAffinity: None
  ports:
  - name: phpmyadmin-service
    protocol: TCP
    port: 80
    targetPort: 80
