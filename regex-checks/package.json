{
  "name": "@raducualexandrumircea/lunaris-regex-checks",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "push": "npm run build && npm publish",
    "build": "rm -rf ./dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@raducualexandrumircea/lunaris-interfaces": "^1.0.0",
    "moment": "^2.30.1",
    "typescript": "^5.4.3"
  },
  "devDependencies": {
    "@types/moment": "^2.13.0"
  }
}
